/* 
A string is said to be beautiful if each letter in the string appears at most as many time as the previous letter in the alphabet within the string; ie: b occurs no more time than a; c occurs no more times than b; etc. Nothe that letter a has no previous letter.

For inputString = "bbbaacdafe" the output should be solution(inputString) = true.
*/